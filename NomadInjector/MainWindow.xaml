<Window x:Class="NomadInjector.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Nomad Injector v1.0" Height="450" Width="400"
        Icon="/NomadIcon.png"
        Background="White" 
        Foreground="Black">

    <Window.Resources>
        <SolidColorBrush x:Key="LightBackground" Color="White"/>
        <SolidColorBrush x:Key="DarkForeground" Color="Black"/>
        <SolidColorBrush x:Key="AccentColor" Color="#00A300"/>
        <SolidColorBrush x:Key="ButtonBackground" Color="#EFEFEF"/>
        <SolidColorBrush x:Key="BorderColor" Color="#CCCCCC"/>

        <Style TargetType="Button">
            <Setter Property="Background" Value="{StaticResource ButtonBackground}"/>
            <Setter Property="Foreground" Value="{StaticResource DarkForeground}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Margin" Value="0,5,0,5"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Width" Value="90"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#DDDDDD"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="GroupBox">
            <Setter Property="Foreground" Value="{StaticResource DarkForeground}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style TargetType="{x:Type Control}">
            <Setter Property="Background" Value="#F8F8F8"/>
            <Setter Property="Foreground" Value="{StaticResource DarkForeground}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="24"/>
            <Style.Triggers>
                <Trigger Property="IsKeyboardFocusWithin" Value="True">
                    <Setter Property="BorderBrush" Value="{StaticResource AccentColor}"/>
                    <Setter Property="BorderThickness" Value="2"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Label">
            <Setter Property="Foreground" Value="{StaticResource DarkForeground}"/>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,10">
            <Label Content="Process Name:" VerticalAlignment="Center" FontWeight="Bold"/>
            <TextBox x:Name="TxtProcessName" Width="150" Margin="5,0,0,0" VerticalAlignment="Center"/>
            <Button x:Name="BtnSelectProcess" Content="Select" Margin="10,0,0,0" Height="30" VerticalAlignment="Center" Click="BtnSelectProcess_Click"/>
        </StackPanel>

        <GroupBox Header="DLL List" Grid.Row="1" Grid.RowSpan="2" Margin="0,10,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                    <Button x:Name="BtnAddDLL" Content="Add DLL" Click="BtnAddDLL_Click"/>
                    <Button x:Name="BtnRemoveDLL" Content="Remove" Click="BtnRemoveDLL_Click"/>
                    <Button x:Name="BtnClearDLLs" Content="Clear" Click="BtnClearDLLs_Click"/>
                    <Button x:Name="BtnSettings" Content="Settings" Click="BtnSettings_Click"/>
                    <Button x:Name="BtnAbout" Content="About" Click="BtnAbout_Click"/>
                </StackPanel>

                <ListView x:Name="LstDLLs" Grid.Column="1" Margin="5" Background="#F8F8F8" BorderBrush="{StaticResource BorderColor}">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn>
                                <GridViewColumnHeader Content="Inject?" Foreground="{StaticResource AccentColor}"/>
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <CheckBox IsChecked="{Binding IsSelected}" HorizontalAlignment="Center"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="DLL Name" Width="250" DisplayMemberBinding="{Binding DisplayName}"/>
                        </GridView>
                    </ListView.View>
                </ListView>
            </Grid>
        </GroupBox>

        <StackPanel Orientation="Horizontal" Grid.Row="3" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button x:Name="BtnInject" Content="Inject" 
                    Width="100" Height="40" 
                    Background="{StaticResource AccentColor}" 
                    Foreground="White" 
                    FontWeight="ExtraBold" 
                    BorderBrush="{StaticResource AccentColor}" 
                    BorderThickness="2"
                    Click="BtnInject_Click">
                <Button.Style>
                    <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                        <Setter Property="Background" Value="{StaticResource AccentColor}"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="Width" Value="100"/>
                        <Setter Property="Height" Value="40"/>
                        <Setter Property="BorderThickness" Value="2"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#007F33"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>
    </Grid>
</Window>